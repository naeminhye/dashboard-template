import React from 'react'
import { Input } from 'semantic-ui-react'
import { Search } from '../../assets/styles';

const SearchBox = ({ placeholder }) => <Search icon='search' placeholder={placeholder} />

export default SearchBox
